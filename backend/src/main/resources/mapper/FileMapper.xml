<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.snstimeline.file.FileMapper">

  <!--
    stored_files には deleted_at が無い（物理削除）。
    論理削除にすると UNIQUE (storage_type, storage_key) と噛み合わず、
    実体を消したのに行だけ残る状態を招くため（docs/04_data_model.md 2.7）。
  -->
  <sql id="fileColumns">
    id, storage_type, storage_key, original_filename, content_type,
    size_bytes, width, height, uploaded_by, created_at
  </sql>

  <!--
    値の埋め込みは必ず #{}（プリペアドステートメント）。
    ${} は文字列置換でSQLインジェクションになるため使わない
    （docs/06_non_functional.md 3.7）。
  -->
  <!--
    StoredFile は record（イミュータブル）なので useGeneratedKeys では採番値を
    書き戻せない。UserMapper.insert と同じく RETURNING で受け取る。
  -->
  <select id="insert" resultType="java.lang.Long">
    INSERT INTO stored_files (
      storage_type, storage_key, original_filename, content_type,
      size_bytes, width, height, uploaded_by
    )
    VALUES (
      #{storageType}, #{storageKey}, #{originalFilename}, #{contentType},
      #{sizeBytes}, #{width}, #{height}, #{uploadedBy}
    )
    RETURNING id
  </select>

  <select id="findById" resultType="com.example.snstimeline.file.StoredFile">
    SELECT <include refid="fileColumns"/>
    FROM stored_files
    WHERE id = #{fileId}
  </select>

</mapper>
